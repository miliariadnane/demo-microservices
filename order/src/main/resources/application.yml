server:
  port: 8003
  servlet:
    context-path: /order
  error:
    include-message: always

spring:
  application:
    name: order
  config:
    import:
      - classpath:shared-application-${spring.profiles.active}.yml
