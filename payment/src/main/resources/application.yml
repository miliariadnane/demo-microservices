server:
  port: 8005
  servlet:
    context-path: /payment
  error:
    include-message: always

spring:
  application:
    name: payment
  config:
    import:
      - classpath:shared-application-${spring.profiles.active}.yml
