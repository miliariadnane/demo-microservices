server:
  port: 8002
  servlet:
    context-path: /product
  error:
    include-message: always

spring:
  application:
    name: product
  config:
    import:
      - classpath:shared-application-${spring.profiles.active}.yml
