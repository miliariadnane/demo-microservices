server:
  port: 8002
  error:
    include-message: always

spring:
  application:
    name: product
  config:
    import:
      - "classpath:shared-application-${spring.profiles.active}.yml"

# Deployment metadata — used by VersionHeaderFilter to identify the running version
app:
  version: ${APP_VERSION:unknown}
